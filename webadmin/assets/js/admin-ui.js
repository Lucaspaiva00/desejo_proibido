export const esc = (v) => String(v ?? "").replace(/[&<>"']/g, m => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
export const num = (v) => Number(v || 0).toLocaleString("pt-BR");
export const brl = (centavos) => new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(centavos||0)/100);
export const dateTime = (v) => v ? new Date(v).toLocaleString("pt-BR") : "-";
export const dateOnly = (v) => v ? new Date(v).toLocaleDateString("pt-BR") : "-";
export function initials(name,email){const s=String(name||email||"U").trim();return s.split(/\s+/).slice(0,2).map(x=>x[0]?.toUpperCase()).join("")||"U"}
export function badge(status){const s=String(status??"").toUpperCase(); let c=""; if(["ATIVA","ATIVO","APROVADA","APROVADO","PAGO","RESOLVIDA","SUCCESS","APPROVED"].includes(s))c="success";else if(["BLOQUEADA","BANIDO","RECUSADO","REPROVADA","ENCERRADA","INATIVO","FAILED","REJECTED"].includes(s))c="danger";else if(["PENDENTE","EM_ANALISE","PENDING"].includes(s))c="warning";else if(["PREMIUM","VIP"].includes(s))c="info";return `<span class="badge-soft ${c}">${esc(status||"-")}</span>`}
export function pageHead(eyebrow,title,sub,actions=""){return `<div class="page-head"><div><div class="eyebrow">${esc(eyebrow)}</div><h1 class="page-title">${esc(title)}</h1><div class="page-sub">${esc(sub)}</div></div><div>${actions}</div></div>`}
export function kpi(icon,label,value,foot="",cls=""){return `<div class="kpi-card ${cls}"><div class="kpi-icon"><i class="fa-solid ${icon}"></i></div><div class="kpi-label">${esc(label)}</div><div class="kpi-value">${value}</div><div class="kpi-foot">${foot}</div></div>`}
export function chartBars(el,data,formatter=(v)=>num(v)){if(!el)return;const arr=data||[];const max=Math.max(1,...arr.map(x=>Number(x.valor||0)));el.innerHTML=`<div class="chart-bars">${arr.map(x=>{const h=Math.max(2,Math.round((Number(x.valor||0)/max)*100));return `<div class="chart-bar" style="height:${h}%" data-tip="${esc(x.data)} • ${esc(formatter(x.valor))}"></div>`}).join("")}</div><div class="chart-axis"><span>${esc(arr[0]?.data||"")}</span><span>${esc(arr[arr.length-1]?.data||"")}</span></div>`}
let toastTimer; export function toast(message){let el=document.getElementById("adminToast");if(!el){el=document.createElement("div");el.id="adminToast";el.className="toast-admin";document.body.appendChild(el)}el.textContent=message;el.classList.add("show");clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove("show"),2600)}
export function setLoading(root){root.innerHTML=`<div class="panel"><div class="skeleton" style="width:38%"></div><div class="skeleton mt-3" style="width:92%;height:90px"></div><div class="skeleton mt-3" style="width:75%"></div></div>`}
export function pager(page,pages,total){return `<div class="pagination-admin"><span>${num(total)} registros • página ${page} de ${pages}</span><div class="d-flex gap-2"><button class="btn-admin sm" data-page="${page-1}" ${page<=1?"disabled":""}>Anterior</button><button class="btn-admin sm" data-page="${page+1}" ${page>=pages?"disabled":""}>Próxima</button></div></div>`}
