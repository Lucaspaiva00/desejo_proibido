import { requireAdmin } from "./auth-guard.js"; import { mountLayout,setTitle } from "./layout.js"; import { apiFetch } from "./api.js"; import { pageHead,num,dateTime,esc,badge,initials,pager,setLoading } from "./admin-ui.js";
mountLayout("usuarios");setTitle("Usuários");await requireAdmin();const root=document.getElementById("pageContent");let state={page:1,q:new URLSearchParams(location.search).get("q")||"",ativo:"",premium:"",creatorStatus:""};
function shell(){root.innerHTML=`${pageHead("Operação","Usuários","Busque, filtre e abra a ficha 360° de qualquer conta.")}<div class="panel"><div class="toolbar"><input id="q" class="form-control" style="max-width:320px" placeholder="Nome, e-mail ou cidade" value="${esc(state.q)}"><select id="ativo" class="form-select" style="max-width:150px"><option value="">Status</option><option value="true">Ativos</option><option value="false">Inativos</option></select><select id="premium" class="form-select" style="max-width:150px"><option value="">Plano</option><option value="true">Premium</option><option value="false">Free</option></select><select id="creator" class="form-select" style="max-width:190px"><option value="">Criadora: todas</option><option>PENDENTE</option><option>APROVADA</option><option>BLOQUEADA</option><option>REPROVADA</option></select><button id="buscar" class="btn-admin primary"><i class="fa-solid fa-search"></i> Buscar</button></div><div id="usersBox"></div></div>`;document.getElementById("ativo").value=state.ativo;document.getElementById("premium").value=state.premium;document.getElementById("creator").value=state.creatorStatus;document.getElementById("buscar").onclick=()=>{state={...state,page:1,q:document.getElementById("q").value.trim(),ativo:document.getElementById("ativo").value,premium:document.getElementById("premium").value,creatorStatus:document.getElementById("creator").value};load()};document.getElementById("q").onkeydown=e=>{if(e.key==="Enter")document.getElementById("buscar").click()}}
async function load(){const box=document.getElementById("usersBox");box.innerHTML=`<div class="empty">Carregando usuários…</div>`;const qs=new URLSearchParams({page:state.page,limit:25});for(const k of ["q","ativo","premium","creatorStatus"])if(state[k])qs.set(k,state[k]);try{const r=await apiFetch(`/admin/v2/usuarios?${qs}`);box.innerHTML=`<div class="table-wrap"><table class="admin-table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Plano</th><th>Saldo</th><th>Atividade</th><th>Status</th><th></th></tr></thead><tbody>${(r.data||[]).map(u=>`<tr><td><div class="user-cell"><div class="avatar">${initials(u.perfil?.nome,u.email)}</div><div><div class="cell-main">${esc(u.perfil?.nome||"Sem nome")}</div><div class="cell-sub">${esc(u.email)}</div></div></div></td><td><div>${esc(u.perfil?.genero||"-")} • ${u.idade??"-"} anos</div><div class="cell-sub">${esc([u.perfil?.cidade,u.perfil?.estado].filter(Boolean).join(" / ")||"-")}</div></td><td>${badge(u.plano)} ${u.creatorStatus!=="NAO_SOLICITADO"?badge(u.creatorStatus):""}</td><td><div>${num(u.wallet?.saldoCreditos)} cr</div><div class="cell-sub">${num(u.minutosDisponiveis)} min</div></td><td><div>${num(u._count?.lives)} lives</div><div class="cell-sub">${num(u._count?.denunciasRecebidas)} denúncias</div></td><td>${u.ativo?badge("ATIVO"):badge("INATIVO")} ${u.banGlobal?.ativo?badge("BANIDO"):""}</td><td><a class="btn-admin sm" href="usuario.html?id=${encodeURIComponent(u.id)}">Abrir <i class="fa-solid fa-arrow-right"></i></a></td></tr>`).join("")||`<tr><td colspan="7" class="empty">Nenhum usuário encontrado.</td></tr>`}</tbody></table></div>${pager(r.page,r.pages,r.total)}`;box.querySelectorAll("[data-page]").forEach(b=>b.onclick=()=>{if(!b.disabled){state.page=Number(b.dataset.page);load()}})}catch(e){box.innerHTML=`<div class="alert-admin">${esc(e.message)}</div>`}}
shell();await load();
